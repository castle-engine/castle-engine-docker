#!/usr/bin/env bash
set -euo pipefail

# chmod -R 'a+rX'     /etc/fpc.cfg* /usr/local/fpclazarus/
# chown -R root:staff /etc/fpc.cfg* /usr/local/fpclazarus/

echo 'No point in fixing permissions inside Docker container, not doing anything.'
